#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int x{};
	cin >> x;
	int ans = 111 * x * x + 11 * x + 1;
	cout << ans << "\n";
	return 0;
}
