#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T{};
	cin >> T;
	while (T--) {
		int a{}, b{};
		char c{};
		cin >> a >> c >> b;
		cout << a + b << "\n";
	}
	return 0;
}
