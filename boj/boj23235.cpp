#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int N{};
	for (int tc = 1; cin >> N, N; ++tc) {
		int x{};
		while (N--) {
			cin >> x;
		}
		cout << "Case " << tc << ": Sorting... done!\n";
	}
	return 0;
}
