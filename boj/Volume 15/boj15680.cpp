#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int N{};
	cin >> N;
	cout << (N ? "Leading the Way to the Future" : "YONSEI") << "\n";
	return 0;
}
