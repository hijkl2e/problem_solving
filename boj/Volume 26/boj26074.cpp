#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int N{}, M{};
	cin >> N >> M;
	cout << (N * M == 2 ? "ChongChong" : "GomGom") << "\n";
	return 0;
}
