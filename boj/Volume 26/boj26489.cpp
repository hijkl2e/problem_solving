#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	string S;
	int ans{};
	while (getline(cin, S)) {
		++ans;
	}
	cout << ans << "\n";
	return 0;
}
