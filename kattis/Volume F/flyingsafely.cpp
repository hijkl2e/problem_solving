#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T{};
	cin >> T;
	while (T--) {
		int N{}, M{};
		cin >> N >> M;
		while (M--) {
			int a{}, b{};
			cin >> a >> b;
		}
		int ans = N - 1;
		cout << ans << "\n";
	}
	return 0;
}
